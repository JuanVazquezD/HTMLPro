import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Tu archivo CSS para Tailwind
import App from './App';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);